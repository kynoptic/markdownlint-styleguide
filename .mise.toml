# Pinned toolchain (managed by mise) — decouples this repo from the
# Homebrew-global runtimes so `brew upgrade` cannot change its versions.
# Supersedes the former .nvmrc (which pinned node 24 but was unenforced).
[tools]
node = "24"
